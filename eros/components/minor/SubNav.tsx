import subnavStyles from "../../styles/minor/subnav.module.css";

const SubNav = () => {
	return (
		<div className={subnavStyles.fadeWrapper}>
			<div className={subnavStyles.notch}></div>
		</div>
	);
};

export default SubNav;
