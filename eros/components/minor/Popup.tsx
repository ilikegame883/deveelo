interface PopProps {
	title: string;
	supertitle?: string;
	message: string;
	button?: string;
}

const Popup = ({ title, supertitle, message, button }: PopProps) => {
	return <div></div>;
};

export default Popup;
