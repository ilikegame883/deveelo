import iconheaderStyles from "../../styles/micro/iconheader.module.css";

const IconHeader = () => {
	return <div className={iconheaderStyles.container}>IconHeader</div>;
};

export default IconHeader;
